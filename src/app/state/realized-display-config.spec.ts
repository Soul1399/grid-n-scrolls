import { RealizedDisplayConfig } from './realized-display-config';

describe('RealizedDisplayConfig', () => {
  it('should create an instance', () => {
    expect(new RealizedDisplayConfig()).toBeTruthy();
  });
});
